// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { CodeableReference } from "../../hl7-fhir-r5-core/CodeableReference";
import type { Extension } from "../../hl7-fhir-r5-core/Extension";
import type { Reference } from "../../hl7-fhir-r5-core/Reference";

import { validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/biologicallyderivedproduct-processing (pkg: hl7.fhir.uv.extensions#5.1.0-snapshot1)
export class BDPProcessingProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/biologicallyderivedproduct-processing"

    private resource: Extension

    constructor (resource: Extension) {
        this.resource = resource
    }

    static from (resource: Extension) : BDPProcessingProfile {
        return new BDPProcessingProfile(resource)
    }

    static createResource () : Extension {
        const resource: Extension = {
            url: "http://hl7.org/fhir/StructureDefinition/biologicallyderivedproduct-processing",
        } as unknown as Extension
        return resource
    }

    static create () : BDPProcessingProfile {
        return BDPProcessingProfile.from(BDPProcessingProfile.createResource())
    }

    toResource () : Extension {
        return this.resource
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value })
        return this
    }

    public setDescription (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "description", valueString: value } as Extension)
        return this
    }

    public setProcedure (value: CodeableReference): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "procedure", valueCodeableReference: value } as Extension)
        return this
    }

    public setAdditive (value: Reference): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "additive", valueReference: value } as Extension)
        return this
    }

    public setTime (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "time[x]", valueDateTime: value } as Extension)
        return this
    }

    public getDescription (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "description")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getDescriptionExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "description")
        return ext
    }

    public getProcedure (): CodeableReference | undefined {
        const ext = this.resource.extension?.find(e => e.url === "procedure")
        return (ext as Record<string, unknown> | undefined)?.valueCodeableReference as CodeableReference | undefined
    }

    public getProcedureExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "procedure")
        return ext
    }

    public getAdditive (): Reference | undefined {
        const ext = this.resource.extension?.find(e => e.url === "additive")
        return (ext as Record<string, unknown> | undefined)?.valueReference as Reference | undefined
    }

    public getAdditiveExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "additive")
        return ext
    }

    public getTime (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "time[x]")
        return (ext as Record<string, unknown> | undefined)?.valueDateTime as string | undefined
    }

    public getTimeExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "time[x]")
        return ext
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "url", "BDPProcessing"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "url", "http://hl7.org/fhir/StructureDefinition/biologicallyderivedproduct-processing", "BDPProcessing"); if (e) errors.push(e) }
        return errors
    }

}

