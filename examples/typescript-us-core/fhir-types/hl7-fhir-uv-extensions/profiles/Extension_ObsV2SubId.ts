// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r5-core/Extension";

import { validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/observation-v2-subid (pkg: hl7.fhir.uv.extensions#5.1.0-snapshot1)
export class ObsV2SubIdProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/observation-v2-subid"

    private resource: Extension

    constructor (resource: Extension) {
        this.resource = resource
    }

    static from (resource: Extension) : ObsV2SubIdProfile {
        return new ObsV2SubIdProfile(resource)
    }

    static createResource () : Extension {
        const resource: Extension = {
            url: "http://hl7.org/fhir/StructureDefinition/observation-v2-subid",
        } as unknown as Extension
        return resource
    }

    static create () : ObsV2SubIdProfile {
        return ObsV2SubIdProfile.from(ObsV2SubIdProfile.createResource())
    }

    toResource () : Extension {
        return this.resource
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value })
        return this
    }

    public setOriginalSubIdentifier (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "original-sub-identifier", valueString: value } as Extension)
        return this
    }

    public setGroup (value: number): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "group", valueDecimal: value } as Extension)
        return this
    }

    public setSequence (value: number): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "sequence", valueDecimal: value } as Extension)
        return this
    }

    public setIdentifier (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "identifier", valueString: value } as Extension)
        return this
    }

    public getOriginalSubIdentifier (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "original-sub-identifier")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getOriginalSubIdentifierExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "original-sub-identifier")
        return ext
    }

    public getGroup (): number | undefined {
        const ext = this.resource.extension?.find(e => e.url === "group")
        return (ext as Record<string, unknown> | undefined)?.valueDecimal as number | undefined
    }

    public getGroupExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "group")
        return ext
    }

    public getSequence (): number | undefined {
        const ext = this.resource.extension?.find(e => e.url === "sequence")
        return (ext as Record<string, unknown> | undefined)?.valueDecimal as number | undefined
    }

    public getSequenceExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "sequence")
        return ext
    }

    public getIdentifier (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "identifier")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getIdentifierExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "identifier")
        return ext
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "url", "ObsV2SubId"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "url", "http://hl7.org/fhir/StructureDefinition/observation-v2-subid", "ObsV2SubId"); if (e) errors.push(e) }
        return errors
    }

}

