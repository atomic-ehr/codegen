// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Coding } from "../../hl7-fhir-r5-core/Coding";
import type { Extension } from "../../hl7-fhir-r5-core/Extension";
import type { Reference } from "../../hl7-fhir-r5-core/Reference";

import { validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

export type OrganizationPortalProfileParams = {
    extension: Extension[];
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/organization-portal (pkg: hl7.fhir.uv.extensions#5.1.0-snapshot1)
export class OrganizationPortalProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/organization-portal"

    private resource: Extension

    constructor (resource: Extension) {
        this.resource = resource
    }

    static from (resource: Extension) : OrganizationPortalProfile {
        return new OrganizationPortalProfile(resource)
    }

    static createResource (args: OrganizationPortalProfileParams) : Extension {
        const resource: Extension = {
            url: "http://hl7.org/fhir/StructureDefinition/organization-portal",
            extension: args.extension,
        } as unknown as Extension
        return resource
    }

    static create (args: OrganizationPortalProfileParams) : OrganizationPortalProfile {
        return OrganizationPortalProfile.from(OrganizationPortalProfile.createResource(args))
    }

    toResource () : Extension {
        return this.resource
    }

    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value })
        return this
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value })
        return this
    }

    public setPortalName (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "portalName", valueString: value } as Extension)
        return this
    }

    public setPortalDescription (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "portalDescription", valueMarkdown: value } as Extension)
        return this
    }

    public setPortalUrl (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "portalUrl", valueUrl: value } as Extension)
        return this
    }

    public setPortalLogo (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "portalLogo", valueUrl: value } as Extension)
        return this
    }

    public setPortalLogoLicenseType (value: Coding): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "portalLogoLicenseType", valueCoding: value } as Extension)
        return this
    }

    public setPortalLogoLicense (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "portalLogoLicense", valueUrl: value } as Extension)
        return this
    }

    public setPortalEndpoint (value: Reference): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "portalEndpoint", valueReference: value } as Extension)
        return this
    }

    public getPortalName (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalName")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getPortalNameExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalName")
        return ext
    }

    public getPortalDescription (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalDescription")
        return (ext as Record<string, unknown> | undefined)?.valueMarkdown as string | undefined
    }

    public getPortalDescriptionExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalDescription")
        return ext
    }

    public getPortalUrl (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalUrl")
        return (ext as Record<string, unknown> | undefined)?.valueUrl as string | undefined
    }

    public getPortalUrlExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalUrl")
        return ext
    }

    public getPortalLogo (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalLogo")
        return (ext as Record<string, unknown> | undefined)?.valueUrl as string | undefined
    }

    public getPortalLogoExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalLogo")
        return ext
    }

    public getPortalLogoLicenseType (): Coding | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalLogoLicenseType")
        return (ext as Record<string, unknown> | undefined)?.valueCoding as Coding | undefined
    }

    public getPortalLogoLicenseTypeExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalLogoLicenseType")
        return ext
    }

    public getPortalLogoLicense (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalLogoLicense")
        return (ext as Record<string, unknown> | undefined)?.valueUrl as string | undefined
    }

    public getPortalLogoLicenseExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalLogoLicense")
        return ext
    }

    public getPortalEndpoint (): Reference | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalEndpoint")
        return (ext as Record<string, unknown> | undefined)?.valueReference as Reference | undefined
    }

    public getPortalEndpointExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "portalEndpoint")
        return ext
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "extension", "OrganizationPortal"); if (e) errors.push(e) }
        { const e = validateRequired(r, "url", "OrganizationPortal"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "url", "http://hl7.org/fhir/StructureDefinition/organization-portal", "OrganizationPortal"); if (e) errors.push(e) }
        return errors
    }

}

