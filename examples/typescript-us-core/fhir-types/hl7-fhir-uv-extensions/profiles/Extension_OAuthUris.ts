// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r5-core/Extension";

import { validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

export type OAuthUrisProfileParams = {
    extension: Extension[];
}

// CanonicalURL: http://fhir-registry.smarthealthit.org/StructureDefinition/oauth-uris (pkg: hl7.fhir.uv.extensions#5.1.0-snapshot1)
export class OAuthUrisProfile {
    static readonly canonicalUrl = "http://fhir-registry.smarthealthit.org/StructureDefinition/oauth-uris"

    private resource: Extension

    constructor (resource: Extension) {
        this.resource = resource
    }

    static from (resource: Extension) : OAuthUrisProfile {
        return new OAuthUrisProfile(resource)
    }

    static createResource (args: OAuthUrisProfileParams) : Extension {
        const resource: Extension = {
            url: "http://fhir-registry.smarthealthit.org/StructureDefinition/oauth-uris",
            extension: args.extension,
        } as unknown as Extension
        return resource
    }

    static create (args: OAuthUrisProfileParams) : OAuthUrisProfile {
        return OAuthUrisProfile.from(OAuthUrisProfile.createResource(args))
    }

    toResource () : Extension {
        return this.resource
    }

    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value })
        return this
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value })
        return this
    }

    public setAuthorize (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "authorize", valueUri: value } as Extension)
        return this
    }

    public setToken (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "token", valueUri: value } as Extension)
        return this
    }

    public setRegister (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "register", valueUri: value } as Extension)
        return this
    }

    public setManage (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "manage", valueUri: value } as Extension)
        return this
    }

    public getAuthorize (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "authorize")
        return (ext as Record<string, unknown> | undefined)?.valueUri as string | undefined
    }

    public getAuthorizeExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "authorize")
        return ext
    }

    public getToken (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "token")
        return (ext as Record<string, unknown> | undefined)?.valueUri as string | undefined
    }

    public getTokenExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "token")
        return ext
    }

    public getRegister (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "register")
        return (ext as Record<string, unknown> | undefined)?.valueUri as string | undefined
    }

    public getRegisterExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "register")
        return ext
    }

    public getManage (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "manage")
        return (ext as Record<string, unknown> | undefined)?.valueUri as string | undefined
    }

    public getManageExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "manage")
        return ext
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "extension", "OAuthUris"); if (e) errors.push(e) }
        { const e = validateRequired(r, "url", "OAuthUris"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "url", "http://fhir-registry.smarthealthit.org/StructureDefinition/oauth-uris", "OAuthUris"); if (e) errors.push(e) }
        return errors
    }

}

