// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r5-core/Extension";
import type { UsageContext } from "../../hl7-fhir-r5-core/UsageContext";

import { validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

export type ObligationProfileParams = {
    extension: Extension[];
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/obligation (pkg: hl7.fhir.uv.extensions#5.1.0-snapshot1)
export class ObligationProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/obligation"

    private resource: Extension

    constructor (resource: Extension) {
        this.resource = resource
    }

    static from (resource: Extension) : ObligationProfile {
        return new ObligationProfile(resource)
    }

    static createResource (args: ObligationProfileParams) : Extension {
        const resource: Extension = {
            url: "http://hl7.org/fhir/StructureDefinition/obligation",
            extension: args.extension,
        } as unknown as Extension
        return resource
    }

    static create (args: ObligationProfileParams) : ObligationProfile {
        return ObligationProfile.from(ObligationProfile.createResource(args))
    }

    toResource () : Extension {
        return this.resource
    }

    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value })
        return this
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value })
        return this
    }

    public setCode (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "code", valueCode: value } as Extension)
        return this
    }

    public setElementId (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "elementId", valueString: value } as Extension)
        return this
    }

    public setActor (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "actor", valueCanonical: value } as Extension)
        return this
    }

    public setDocumentation (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "documentation", valueMarkdown: value } as Extension)
        return this
    }

    public setUsage (value: UsageContext): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "usage", valueUsageContext: value } as Extension)
        return this
    }

    public setFilter (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "filter", valueString: value } as Extension)
        return this
    }

    public setFilterDocumentation (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "filterDocumentation", valueString: value } as Extension)
        return this
    }

    public setProcess (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "process", valueCanonical: value } as Extension)
        return this
    }

    public getCode (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "code")
        return (ext as Record<string, unknown> | undefined)?.valueCode as string | undefined
    }

    public getCodeExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "code")
        return ext
    }

    public getElementId (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "elementId")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getElementIdExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "elementId")
        return ext
    }

    public getActor (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "actor")
        return (ext as Record<string, unknown> | undefined)?.valueCanonical as string | undefined
    }

    public getActorExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "actor")
        return ext
    }

    public getDocumentation (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "documentation")
        return (ext as Record<string, unknown> | undefined)?.valueMarkdown as string | undefined
    }

    public getDocumentationExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "documentation")
        return ext
    }

    public getUsage (): UsageContext | undefined {
        const ext = this.resource.extension?.find(e => e.url === "usage")
        return (ext as Record<string, unknown> | undefined)?.valueUsageContext as UsageContext | undefined
    }

    public getUsageExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "usage")
        return ext
    }

    public getFilter (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "filter")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getFilterExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "filter")
        return ext
    }

    public getFilterDocumentation (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "filterDocumentation")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getFilterDocumentationExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "filterDocumentation")
        return ext
    }

    public getProcess (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "process")
        return (ext as Record<string, unknown> | undefined)?.valueCanonical as string | undefined
    }

    public getProcessExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "process")
        return ext
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "extension", "Obligation"); if (e) errors.push(e) }
        { const e = validateRequired(r, "url", "Obligation"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "url", "http://hl7.org/fhir/StructureDefinition/obligation", "Obligation"); if (e) errors.push(e) }
        return errors
    }

}

