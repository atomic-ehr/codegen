// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r5-core/Extension";

import { validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

export type VersionSpecificValueProfileParams = {
    extension: Extension[];
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/version-specific-value (pkg: hl7.fhir.uv.extensions#5.1.0-snapshot1)
export class VersionSpecificValueProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/version-specific-value"

    private resource: Extension

    constructor (resource: Extension) {
        this.resource = resource
    }

    static from (resource: Extension) : VersionSpecificValueProfile {
        return new VersionSpecificValueProfile(resource)
    }

    static createResource (args: VersionSpecificValueProfileParams) : Extension {
        const resource: Extension = {
            url: "http://hl7.org/fhir/StructureDefinition/version-specific-value",
            extension: args.extension,
        } as unknown as Extension
        return resource
    }

    static create (args: VersionSpecificValueProfileParams) : VersionSpecificValueProfile {
        return VersionSpecificValueProfile.from(VersionSpecificValueProfile.createResource(args))
    }

    toResource () : Extension {
        return this.resource
    }

    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value })
        return this
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value })
        return this
    }

    public setValue (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "value", valueString: value } as Extension)
        return this
    }

    public setStartFhirVersion (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "startFhirVersion", valueCode: value } as Extension)
        return this
    }

    public setEndFhirVersion (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "endFhirVersion", valueCode: value } as Extension)
        return this
    }

    public getValue (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "value")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getValueExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "value")
        return ext
    }

    public getStartFhirVersion (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "startFhirVersion")
        return (ext as Record<string, unknown> | undefined)?.valueCode as string | undefined
    }

    public getStartFhirVersionExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "startFhirVersion")
        return ext
    }

    public getEndFhirVersion (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "endFhirVersion")
        return (ext as Record<string, unknown> | undefined)?.valueCode as string | undefined
    }

    public getEndFhirVersionExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "endFhirVersion")
        return ext
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "extension", "VersionSpecificValue"); if (e) errors.push(e) }
        { const e = validateRequired(r, "url", "VersionSpecificValue"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "url", "http://hl7.org/fhir/StructureDefinition/version-specific-value", "VersionSpecificValue"); if (e) errors.push(e) }
        return errors
    }

}

