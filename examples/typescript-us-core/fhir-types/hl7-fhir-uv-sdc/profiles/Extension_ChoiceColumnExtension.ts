// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Quantity } from "../../hl7-fhir-r4-core/Quantity";

import { validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

export type ChoiceColumnExtensionProfileParams = {
    extension: Extension[];
}

// CanonicalURL: http://hl7.org/fhir/uv/sdc/StructureDefinition/sdc-questionnaire-choiceColumn (pkg: hl7.fhir.uv.sdc#3.0.0)
export class ChoiceColumnExtensionProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/uv/sdc/StructureDefinition/sdc-questionnaire-choiceColumn"

    private resource: Extension

    constructor (resource: Extension) {
        this.resource = resource
    }

    static from (resource: Extension) : ChoiceColumnExtensionProfile {
        return new ChoiceColumnExtensionProfile(resource)
    }

    static createResource (args: ChoiceColumnExtensionProfileParams) : Extension {
        const resource: Extension = {
            url: "http://hl7.org/fhir/uv/sdc/StructureDefinition/sdc-questionnaire-choiceColumn",
            extension: args.extension,
        } as unknown as Extension
        return resource
    }

    static create (args: ChoiceColumnExtensionProfileParams) : ChoiceColumnExtensionProfile {
        return ChoiceColumnExtensionProfile.from(ChoiceColumnExtensionProfile.createResource(args))
    }

    toResource () : Extension {
        return this.resource
    }

    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value })
        return this
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value })
        return this
    }

    public setPath (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "path", valueString: value } as Extension)
        return this
    }

    public setLabel (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "label", valueString: value } as Extension)
        return this
    }

    public setWidth (value: Quantity): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "width", valueQuantity: value } as Extension)
        return this
    }

    public setForDisplay (value: boolean): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "forDisplay", valueBoolean: value } as Extension)
        return this
    }

    public getPath (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "path")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getPathExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "path")
        return ext
    }

    public getLabel (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "label")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getLabelExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "label")
        return ext
    }

    public getWidth (): Quantity | undefined {
        const ext = this.resource.extension?.find(e => e.url === "width")
        return (ext as Record<string, unknown> | undefined)?.valueQuantity as Quantity | undefined
    }

    public getWidthExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "width")
        return ext
    }

    public getForDisplay (): boolean | undefined {
        const ext = this.resource.extension?.find(e => e.url === "forDisplay")
        return (ext as Record<string, unknown> | undefined)?.valueBoolean as boolean | undefined
    }

    public getForDisplayExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "forDisplay")
        return ext
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "extension", "ChoiceColumnExtension"); if (e) errors.push(e) }
        { const e = validateRequired(r, "url", "ChoiceColumnExtension"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "url", "http://hl7.org/fhir/uv/sdc/StructureDefinition/sdc-questionnaire-choiceColumn", "ChoiceColumnExtension"); if (e) errors.push(e) }
        return errors
    }

}

