// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Attachment } from "../../hl7-fhir-r4-core/Attachment";
import type { CodeableConcept } from "../../hl7-fhir-r4-core/CodeableConcept";
import type { Library } from "../../hl7-fhir-r4-core/Library";

export type SDCLibrary_Content_CqlContentSliceInput = Omit<Attachment, "contentType"> & Required<Pick<Attachment, "data">>;
export type SDCLibrary_Content_FhirpathContentSliceInput = Omit<Attachment, "contentType"> & Required<Pick<Attachment, "data">>;
export type SDCLibrary_Content_QueryContentSliceInput = Omit<Attachment, "contentType"> & Required<Pick<Attachment, "data">>;

import { applySliceMatch, matchesSlice, extractSliceSimplified, validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

// CanonicalURL: http://hl7.org/fhir/uv/sdc/StructureDefinition/sdc-library (pkg: hl7.fhir.uv.sdc#3.0.0)
export class SDCLibraryProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/uv/sdc/StructureDefinition/sdc-library"

    private resource: Library

    constructor (resource: Library) {
        this.resource = resource
        const r = resource as unknown as Record<string, unknown>
        const meta = (r.meta ??= {}) as Record<string, unknown>
        const profiles = (meta.profile ??= []) as string[]
        if (!profiles.includes("http://hl7.org/fhir/uv/sdc/StructureDefinition/sdc-library")) profiles.push("http://hl7.org/fhir/uv/sdc/StructureDefinition/sdc-library")
    }

    static from (resource: Library) : SDCLibraryProfile {
        return new SDCLibraryProfile(resource)
    }

    static createResource () : Library {
        const resource: Library = {
            resourceType: "Library",
            type: {"coding":[{"system":"http://terminology.hl7.org/CodeSystem/library-type","code":"logic-library","display":"Logic Library"}]},
            meta: { profile: ["http://hl7.org/fhir/uv/sdc/StructureDefinition/sdc-library"] },
        } as unknown as Library
        return resource
    }

    static create () : SDCLibraryProfile {
        return SDCLibraryProfile.from(SDCLibraryProfile.createResource())
    }

    toResource () : Library {
        return this.resource
    }

    getType () : CodeableConcept<("logic-library" | "model-definition" | "asset-collection" | "module-definition" | string)> | undefined {
        return this.resource.type as CodeableConcept<("logic-library" | "model-definition" | "asset-collection" | "module-definition" | string)> | undefined
    }

    setType (value: CodeableConcept<("logic-library" | "model-definition" | "asset-collection" | "module-definition" | string)>) : this {
        Object.assign(this.resource, { type: value })
        return this
    }

    public setCqlContent (input: SDCLibrary_Content_CqlContentSliceInput): this {
        const match = {"contentType":"text/cql"} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as Attachment
        const list = (this.resource.content ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setFhirpathContent (input: SDCLibrary_Content_FhirpathContentSliceInput): this {
        const match = {"contentType":"text/fhirpath"} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as Attachment
        const list = (this.resource.content ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setQueryContent (input: SDCLibrary_Content_QueryContentSliceInput): this {
        const match = {"contentType":"application/x-fhir-query"} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as Attachment
        const list = (this.resource.content ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public getCqlContent (): SDCLibrary_Content_CqlContentSliceInput | undefined {
        const match = {"contentType":"text/cql"} as Record<string, unknown>
        const list = this.resource.content
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["contentType"]) as SDCLibrary_Content_CqlContentSliceInput
    }

    public getCqlContentRaw (): Attachment | undefined {
        const match = {"contentType":"text/cql"} as Record<string, unknown>
        const list = this.resource.content
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getFhirpathContent (): SDCLibrary_Content_FhirpathContentSliceInput | undefined {
        const match = {"contentType":"text/fhirpath"} as Record<string, unknown>
        const list = this.resource.content
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["contentType"]) as SDCLibrary_Content_FhirpathContentSliceInput
    }

    public getFhirpathContentRaw (): Attachment | undefined {
        const match = {"contentType":"text/fhirpath"} as Record<string, unknown>
        const list = this.resource.content
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getQueryContent (): SDCLibrary_Content_QueryContentSliceInput | undefined {
        const match = {"contentType":"application/x-fhir-query"} as Record<string, unknown>
        const list = this.resource.content
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["contentType"]) as SDCLibrary_Content_QueryContentSliceInput
    }

    public getQueryContentRaw (): Attachment | undefined {
        const match = {"contentType":"application/x-fhir-query"} as Record<string, unknown>
        const list = this.resource.content
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "type", "SDCLibrary"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "type", {"coding":[{"system":"http://terminology.hl7.org/CodeSystem/library-type","code":"logic-library","display":"Logic Library"}]}, "SDCLibrary"); if (e) errors.push(e) }
        errors.push(...validateSliceCardinality(r["content"] as unknown[] | undefined, {"contentType":"text/cql"}, "cqlContent", 0, 1, "SDCLibrary.content"))
        errors.push(...validateSliceCardinality(r["content"] as unknown[] | undefined, {"contentType":"text/fhirpath"}, "fhirpathContent", 0, 1, "SDCLibrary.content"))
        errors.push(...validateSliceCardinality(r["content"] as unknown[] | undefined, {"contentType":"application/x-fhir-query"}, "queryContent", 0, 1, "SDCLibrary.content"))
        return errors
    }

}

