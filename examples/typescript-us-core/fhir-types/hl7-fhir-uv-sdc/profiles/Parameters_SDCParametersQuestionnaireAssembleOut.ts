// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Parameters } from "../../hl7-fhir-r4-core/Parameters";
import type { ParametersParameter } from "../../hl7-fhir-r4-core/Parameters";

export type SDCParametersQuestionnaireAssembleOut_Parameter_Return_SliceInput = Omit<ParametersParameter, "name">;
export type SDCParametersQuestionnaireAssembleOut_Parameter_PartSliceInput = Omit<ParametersParameter, "name">;

import { applySliceMatch, matchesSlice, extractSliceSimplified, validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

export type SDCParametersQuestionnaireAssembleOutProfileParams = {
    parameter?: ParametersParameter[];
}

// CanonicalURL: http://hl7.org/fhir/uv/sdc/StructureDefinition/parameters-questionnaire-assemble-out (pkg: hl7.fhir.uv.sdc#3.0.0)
export class SDCParametersQuestionnaireAssembleOutProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/uv/sdc/StructureDefinition/parameters-questionnaire-assemble-out"

    private resource: Parameters

    constructor (resource: Parameters) {
        this.resource = resource
        const r = resource as unknown as Record<string, unknown>
        const meta = (r.meta ??= {}) as Record<string, unknown>
        const profiles = (meta.profile ??= []) as string[]
        if (!profiles.includes("http://hl7.org/fhir/uv/sdc/StructureDefinition/parameters-questionnaire-assemble-out")) profiles.push("http://hl7.org/fhir/uv/sdc/StructureDefinition/parameters-questionnaire-assemble-out")
    }

    static from (resource: Parameters) : SDCParametersQuestionnaireAssembleOutProfile {
        return new SDCParametersQuestionnaireAssembleOutProfile(resource)
    }

    static createResource (args: SDCParametersQuestionnaireAssembleOutProfileParams) : Parameters {
        const parameterDefaults = [{"name":"return"}] as unknown[]
        const parameterWithDefaults = [...(args.parameter ?? [])] as unknown[]
        if (!parameterWithDefaults.some(item => matchesSlice(item, {"name":"return"} as Record<string, unknown>))) parameterWithDefaults.push(parameterDefaults[0]!)
        const resource: Parameters = {
            resourceType: "Parameters",
            parameter: parameterWithDefaults,
            meta: { profile: ["http://hl7.org/fhir/uv/sdc/StructureDefinition/parameters-questionnaire-assemble-out"] },
        } as unknown as Parameters
        return resource
    }

    static create (args: SDCParametersQuestionnaireAssembleOutProfileParams) : SDCParametersQuestionnaireAssembleOutProfile {
        return SDCParametersQuestionnaireAssembleOutProfile.from(SDCParametersQuestionnaireAssembleOutProfile.createResource(args))
    }

    toResource () : Parameters {
        return this.resource
    }

    getParameter () : ParametersParameter[] | undefined {
        return this.resource.parameter as ParametersParameter[] | undefined
    }

    setParameter (value: ParametersParameter[]) : this {
        Object.assign(this.resource, { parameter: value })
        return this
    }

    public setReturn_ (input?: SDCParametersQuestionnaireAssembleOut_Parameter_Return_SliceInput): this {
        const match = {"name":"return"} as Record<string, unknown>
        const value = applySliceMatch((input ?? {}) as Record<string, unknown>, match) as unknown as ParametersParameter
        const list = (this.resource.parameter ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setPart (input?: SDCParametersQuestionnaireAssembleOut_Parameter_PartSliceInput): this {
        const match = {"name":"outcome"} as Record<string, unknown>
        const value = applySliceMatch((input ?? {}) as Record<string, unknown>, match) as unknown as ParametersParameter
        const list = (this.resource.parameter ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public getReturn_ (): SDCParametersQuestionnaireAssembleOut_Parameter_Return_SliceInput | undefined {
        const match = {"name":"return"} as Record<string, unknown>
        const list = this.resource.parameter
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["name"]) as SDCParametersQuestionnaireAssembleOut_Parameter_Return_SliceInput
    }

    public getReturn_Raw (): ParametersParameter | undefined {
        const match = {"name":"return"} as Record<string, unknown>
        const list = this.resource.parameter
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getPart (): SDCParametersQuestionnaireAssembleOut_Parameter_PartSliceInput | undefined {
        const match = {"name":"outcome"} as Record<string, unknown>
        const list = this.resource.parameter
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["name"]) as SDCParametersQuestionnaireAssembleOut_Parameter_PartSliceInput
    }

    public getPartRaw (): ParametersParameter | undefined {
        const match = {"name":"outcome"} as Record<string, unknown>
        const list = this.resource.parameter
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        errors.push(...validateSliceCardinality(r["parameter"] as unknown[] | undefined, {"name":"return"}, "return", 1, 1, "SDCParametersQuestionnaireAssembleOut.parameter"))
        errors.push(...validateSliceCardinality(r["parameter"] as unknown[] | undefined, {"name":"outcome"}, "part", 0, 1, "SDCParametersQuestionnaireAssembleOut.parameter"))
        return errors
    }

}

