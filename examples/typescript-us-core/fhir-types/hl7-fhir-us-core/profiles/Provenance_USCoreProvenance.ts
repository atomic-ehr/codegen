// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Provenance } from "../../hl7-fhir-r4-core/Provenance";
import type { ProvenanceAgent } from "../../hl7-fhir-r4-core/Provenance";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";

export type USCoreProvenance_Agent_ProvenanceAuthorSliceInput = Omit<ProvenanceAgent, "type">;
export type USCoreProvenance_Agent_ProvenanceTransmitterSliceInput = Omit<ProvenanceAgent, "type">;

import { applySliceMatch, matchesSlice, extractSliceSimplified, validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

export type USCoreProvenanceProfileParams = {
    target: Reference<"Resource">[];
    recorded: string;
    agent: ProvenanceAgent[];
}

// CanonicalURL: http://hl7.org/fhir/us/core/StructureDefinition/us-core-provenance (pkg: hl7.fhir.us.core#8.0.1)
export class USCoreProvenanceProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/us/core/StructureDefinition/us-core-provenance"

    private resource: Provenance

    constructor (resource: Provenance) {
        this.resource = resource
        const r = resource as unknown as Record<string, unknown>
        const meta = (r.meta ??= {}) as Record<string, unknown>
        const profiles = (meta.profile ??= []) as string[]
        if (!profiles.includes("http://hl7.org/fhir/us/core/StructureDefinition/us-core-provenance")) profiles.push("http://hl7.org/fhir/us/core/StructureDefinition/us-core-provenance")
    }

    static from (resource: Provenance) : USCoreProvenanceProfile {
        return new USCoreProvenanceProfile(resource)
    }

    static createResource (args: USCoreProvenanceProfileParams) : Provenance {
        const resource: Provenance = {
            resourceType: "Provenance",
            target: args.target,
            recorded: args.recorded,
            agent: args.agent,
            meta: { profile: ["http://hl7.org/fhir/us/core/StructureDefinition/us-core-provenance"] },
        } as unknown as Provenance
        return resource
    }

    static create (args: USCoreProvenanceProfileParams) : USCoreProvenanceProfile {
        return USCoreProvenanceProfile.from(USCoreProvenanceProfile.createResource(args))
    }

    toResource () : Provenance {
        return this.resource
    }

    getTarget () : Reference<"Resource">[] | undefined {
        return this.resource.target as Reference<"Resource">[] | undefined
    }

    setTarget (value: Reference<"Resource">[]) : this {
        Object.assign(this.resource, { target: value })
        return this
    }

    getRecorded () : string | undefined {
        return this.resource.recorded as string | undefined
    }

    setRecorded (value: string) : this {
        Object.assign(this.resource, { recorded: value })
        return this
    }

    getAgent () : ProvenanceAgent[] | undefined {
        return this.resource.agent as ProvenanceAgent[] | undefined
    }

    setAgent (value: ProvenanceAgent[]) : this {
        Object.assign(this.resource, { agent: value })
        return this
    }

    public setProvenanceAuthor (input?: USCoreProvenance_Agent_ProvenanceAuthorSliceInput): this {
        const match = {"type":{"coding":[{"system":"http://terminology.hl7.org/CodeSystem/provenance-participant-type","code":"author"}]}} as Record<string, unknown>
        const value = applySliceMatch((input ?? {}) as Record<string, unknown>, match) as unknown as ProvenanceAgent
        const list = (this.resource.agent ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setProvenanceTransmitter (input?: USCoreProvenance_Agent_ProvenanceTransmitterSliceInput): this {
        const match = {"type":{"coding":[{"system":"http://hl7.org/fhir/us/core/CodeSystem/us-core-provenance-participant-type","code":"transmitter"}]}} as Record<string, unknown>
        const value = applySliceMatch((input ?? {}) as Record<string, unknown>, match) as unknown as ProvenanceAgent
        const list = (this.resource.agent ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public getProvenanceAuthor (): USCoreProvenance_Agent_ProvenanceAuthorSliceInput | undefined {
        const match = {"type":{"coding":[{"system":"http://terminology.hl7.org/CodeSystem/provenance-participant-type","code":"author"}]}} as Record<string, unknown>
        const list = this.resource.agent
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["type"]) as USCoreProvenance_Agent_ProvenanceAuthorSliceInput
    }

    public getProvenanceAuthorRaw (): ProvenanceAgent | undefined {
        const match = {"type":{"coding":[{"system":"http://terminology.hl7.org/CodeSystem/provenance-participant-type","code":"author"}]}} as Record<string, unknown>
        const list = this.resource.agent
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getProvenanceTransmitter (): USCoreProvenance_Agent_ProvenanceTransmitterSliceInput | undefined {
        const match = {"type":{"coding":[{"system":"http://hl7.org/fhir/us/core/CodeSystem/us-core-provenance-participant-type","code":"transmitter"}]}} as Record<string, unknown>
        const list = this.resource.agent
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["type"]) as USCoreProvenance_Agent_ProvenanceTransmitterSliceInput
    }

    public getProvenanceTransmitterRaw (): ProvenanceAgent | undefined {
        const match = {"type":{"coding":[{"system":"http://hl7.org/fhir/us/core/CodeSystem/us-core-provenance-participant-type","code":"transmitter"}]}} as Record<string, unknown>
        const list = this.resource.agent
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "target", "USCoreProvenance"); if (e) errors.push(e) }
        { const e = validateReference(r["target"], ["Resource"], "target", "USCoreProvenance"); if (e) errors.push(e) }
        { const e = validateRequired(r, "recorded", "USCoreProvenance"); if (e) errors.push(e) }
        { const e = validateRequired(r, "agent", "USCoreProvenance"); if (e) errors.push(e) }
        return errors
    }

}

