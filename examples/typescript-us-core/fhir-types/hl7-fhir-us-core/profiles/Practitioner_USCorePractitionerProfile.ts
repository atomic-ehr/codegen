// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { HumanName } from "../../hl7-fhir-r4-core/HumanName";
import type { Identifier } from "../../hl7-fhir-r4-core/Identifier";
import type { Practitioner } from "../../hl7-fhir-r4-core/Practitioner";

export interface USCorePractitionerProfile extends Practitioner {
    identifier: Identifier[];
    name: HumanName[];
}

export type USCorePractitionerProfile_Identifier_NPISliceInput = Omit<Identifier, "system">;

import { applySliceMatch, matchesSlice, extractSliceSimplified, validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

export type USCorePractitionerProfileProfileParams = {
    identifier: Identifier[];
    name: HumanName[];
}

// CanonicalURL: http://hl7.org/fhir/us/core/StructureDefinition/us-core-practitioner (pkg: hl7.fhir.us.core#8.0.1)
export class USCorePractitionerProfileProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/us/core/StructureDefinition/us-core-practitioner"

    private resource: Practitioner

    constructor (resource: Practitioner) {
        this.resource = resource
        const r = resource as unknown as Record<string, unknown>
        const meta = (r.meta ??= {}) as Record<string, unknown>
        const profiles = (meta.profile ??= []) as string[]
        if (!profiles.includes("http://hl7.org/fhir/us/core/StructureDefinition/us-core-practitioner")) profiles.push("http://hl7.org/fhir/us/core/StructureDefinition/us-core-practitioner")
    }

    static from (resource: Practitioner) : USCorePractitionerProfileProfile {
        return new USCorePractitionerProfileProfile(resource)
    }

    static createResource (args: USCorePractitionerProfileProfileParams) : Practitioner {
        const resource: Practitioner = {
            resourceType: "Practitioner",
            identifier: args.identifier,
            name: args.name,
            meta: { profile: ["http://hl7.org/fhir/us/core/StructureDefinition/us-core-practitioner"] },
        } as unknown as Practitioner
        return resource
    }

    static create (args: USCorePractitionerProfileProfileParams) : USCorePractitionerProfileProfile {
        return USCorePractitionerProfileProfile.from(USCorePractitionerProfileProfile.createResource(args))
    }

    toResource () : Practitioner {
        return this.resource
    }

    getIdentifier () : Identifier[] | undefined {
        return this.resource.identifier as Identifier[] | undefined
    }

    setIdentifier (value: Identifier[]) : this {
        Object.assign(this.resource, { identifier: value })
        return this
    }

    getName () : HumanName[] | undefined {
        return this.resource.name as HumanName[] | undefined
    }

    setName (value: HumanName[]) : this {
        Object.assign(this.resource, { name: value })
        return this
    }

    toProfile () : USCorePractitionerProfile {
        return this.resource as USCorePractitionerProfile
    }

    public setNPI (input?: USCorePractitionerProfile_Identifier_NPISliceInput): this {
        const match = {"system":"http://hl7.org/fhir/sid/us-npi"} as Record<string, unknown>
        const value = applySliceMatch((input ?? {}) as Record<string, unknown>, match) as unknown as Identifier
        const list = (this.resource.identifier ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public getNPI (): USCorePractitionerProfile_Identifier_NPISliceInput | undefined {
        const match = {"system":"http://hl7.org/fhir/sid/us-npi"} as Record<string, unknown>
        const list = this.resource.identifier
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["system"]) as USCorePractitionerProfile_Identifier_NPISliceInput
    }

    public getNPIRaw (): Identifier | undefined {
        const match = {"system":"http://hl7.org/fhir/sid/us-npi"} as Record<string, unknown>
        const list = this.resource.identifier
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "identifier", "USCorePractitionerProfile"); if (e) errors.push(e) }
        { const e = validateRequired(r, "name", "USCorePractitionerProfile"); if (e) errors.push(e) }
        return errors
    }

}

