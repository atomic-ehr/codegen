// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Bundle } from "../../hl7-fhir-r5-core/Bundle";
import type { BundleEntry } from "../../hl7-fhir-r5-core/Bundle";

export type SearchSetBundle_Entry_OperationOutcomeSliceInput = Omit<BundleEntry, "search"> & Required<Pick<BundleEntry, "fullUrl" | "resource">>;

import { applySliceMatch, matchesSlice, extractSliceSimplified, validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/search-set-bundle (pkg: hl7.fhir.r5.core#5.0.0)
export class SearchSetBundleProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/search-set-bundle"

    private resource: Bundle

    constructor (resource: Bundle) {
        this.resource = resource
        const r = resource as unknown as Record<string, unknown>
        const meta = (r.meta ??= {}) as Record<string, unknown>
        const profiles = (meta.profile ??= []) as string[]
        if (!profiles.includes("http://hl7.org/fhir/StructureDefinition/search-set-bundle")) profiles.push("http://hl7.org/fhir/StructureDefinition/search-set-bundle")
    }

    static from (resource: Bundle) : SearchSetBundleProfile {
        return new SearchSetBundleProfile(resource)
    }

    static createResource () : Bundle {
        const resource: Bundle = {
            resourceType: "Bundle",
            type: "searchset",
            meta: { profile: ["http://hl7.org/fhir/StructureDefinition/search-set-bundle"] },
        } as unknown as Bundle
        return resource
    }

    static create () : SearchSetBundleProfile {
        return SearchSetBundleProfile.from(SearchSetBundleProfile.createResource())
    }

    toResource () : Bundle {
        return this.resource
    }

    getType () : ("document" | "message" | "transaction" | "transaction-response" | "batch" | "batch-response" | "history" | "searchset" | "collection") | undefined {
        return this.resource.type as ("document" | "message" | "transaction" | "transaction-response" | "batch" | "batch-response" | "history" | "searchset" | "collection") | undefined
    }

    setType (value: ("document" | "message" | "transaction" | "transaction-response" | "batch" | "batch-response" | "history" | "searchset" | "collection")) : this {
        Object.assign(this.resource, { type: value })
        return this
    }

    public setOperationOutcome (input: SearchSetBundle_Entry_OperationOutcomeSliceInput): this {
        const match = {"search":{"mode":"outcome"}} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as BundleEntry
        const list = (this.resource.entry ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public getOperationOutcome (): SearchSetBundle_Entry_OperationOutcomeSliceInput | undefined {
        const match = {"search":{"mode":"outcome"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["search"]) as SearchSetBundle_Entry_OperationOutcomeSliceInput
    }

    public getOperationOutcomeRaw (): BundleEntry | undefined {
        const match = {"search":{"mode":"outcome"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "type", "SearchSetBundle"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "type", "searchset", "SearchSetBundle"); if (e) errors.push(e) }
        { const e = validateEnum(r["type"], ["document","message","transaction","transaction-response","batch","batch-response","history","searchset","collection"], "type", "SearchSetBundle"); if (e) errors.push(e) }
        errors.push(...validateSliceCardinality(r["entry"] as unknown[] | undefined, {"search":{"mode":"outcome"}}, "operationOutcome", 0, 1, "SearchSetBundle.entry"))
        return errors
    }

}

