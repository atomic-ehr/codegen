// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r5-core/Extension";
import type { ValueSet } from "../../hl7-fhir-r5-core/ValueSet";

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/computablevalueset
export class ComputableValueSetProfile {
    private resource: ValueSet

    constructor (resource?: ValueSet) {
        this.resource = resource ?? ({ resourceType: "ValueSet" } as ValueSet)
    }

    toResource () : ValueSet {
        return this.resource
    }

    public setKnowledgeRepresentationLevel (value: Omit<Extension, "url">): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeRepresentationLevel", ...value })
        return this
    }

    public setAuthoritativeSource (value: Omit<Extension, "url">): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/valueset-authoritativeSource", ...value })
        return this
    }

    public setRulesText (value: Omit<Extension, "url">): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/valueset-rules-text", ...value })
        return this
    }

    public setExpression (value: Omit<Extension, "url">): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/valueset-expression", ...value })
        return this
    }

    public setSupplement (value: Omit<Extension, "url">): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/valueset-supplement", ...value })
        return this
    }

    public resetKnowledgeRepresentationLevel (): this {
        const list = this.resource.extension
        if (list) {
            const index = list.findIndex((e) => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeRepresentationLevel")
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

    public resetAuthoritativeSource (): this {
        const list = this.resource.extension
        if (list) {
            const index = list.findIndex((e) => e.url === "http://hl7.org/fhir/StructureDefinition/valueset-authoritativeSource")
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

    public resetRulesText (): this {
        const list = this.resource.extension
        if (list) {
            const index = list.findIndex((e) => e.url === "http://hl7.org/fhir/StructureDefinition/valueset-rules-text")
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

    public resetExpression (): this {
        const list = this.resource.extension
        if (list) {
            const index = list.findIndex((e) => e.url === "http://hl7.org/fhir/StructureDefinition/valueset-expression")
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

    public resetSupplement (): this {
        const list = this.resource.extension
        if (list) {
            const index = list.findIndex((e) => e.url === "http://hl7.org/fhir/StructureDefinition/valueset-supplement")
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

}

