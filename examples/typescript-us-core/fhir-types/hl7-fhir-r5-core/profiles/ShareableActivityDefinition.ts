// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { ActivityDefinition } from "../../hl7-fhir-r5-core/ActivityDefinition";
import type { Extension } from "../../hl7-fhir-r5-core/Extension";

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/shareableactivitydefinition
export class ShareableActivityDefinitionProfile {
    private resource: ActivityDefinition

    constructor (resource?: ActivityDefinition) {
        this.resource = resource ?? ({ resourceType: "ActivityDefinition" } as ActivityDefinition)
    }

    toResource () : ActivityDefinition {
        return this.resource
    }

    public setKnowledgeCapability (value: Omit<Extension, "url">): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeCapability", ...value })
        return this
    }

    public setKnowledgeRepresentationLevel (value: Omit<Extension, "url">): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeRepresentationLevel", ...value })
        return this
    }

    public setArtifactComment (value: Omit<Extension, "url">): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/cqf-artifactComment", ...value })
        return this
    }

    public resetKnowledgeCapability (): this {
        const list = this.resource.extension
        if (list) {
            const index = list.findIndex((e) => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeCapability")
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

    public resetKnowledgeRepresentationLevel (): this {
        const list = this.resource.extension
        if (list) {
            const index = list.findIndex((e) => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeRepresentationLevel")
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

    public resetArtifactComment (): this {
        const list = this.resource.extension
        if (list) {
            const index = list.findIndex((e) => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-artifactComment")
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

}

