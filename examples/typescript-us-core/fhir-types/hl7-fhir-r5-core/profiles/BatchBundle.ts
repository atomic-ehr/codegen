// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Bundle } from "../../hl7-fhir-r5-core/Bundle";
import type { BundleEntry } from "../../hl7-fhir-r5-core/Bundle";

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/batch-bundle
export type BatchBundle_Entry_PutSliceInput = Omit<BundleEntry, "request"> & Required<Pick<BundleEntry, "fullUrl" | "resource">>;
export type BatchBundle_Entry_PostSliceInput = Omit<BundleEntry, "request"> & Required<Pick<BundleEntry, "resource">>;
export type BatchBundle_Entry_GetSliceInput = Omit<BundleEntry, "request"> & Required<Pick<BundleEntry, "fullUrl">>;
export type BatchBundle_Entry_DeleteSliceInput = Omit<BundleEntry, "request"> & Required<Pick<BundleEntry, "fullUrl">>;
export type BatchBundle_Entry_PatchSliceInput = Omit<BundleEntry, "request"> & Required<Pick<BundleEntry, "fullUrl" | "resource">>;
export type BatchBundle_Entry_HeadSliceInput = Omit<BundleEntry, "request"> & Required<Pick<BundleEntry, "fullUrl">>;

import { applySliceMatch, matchesSlice, extractSliceSimplified } from "../../profile-helpers";

export class BatchBundleProfile {
    private resource: Bundle

    constructor (resource: Bundle) {
        this.resource = resource
    }

    toResource () : Bundle {
        return this.resource
    }

    public setPut (input: BatchBundle_Entry_PutSliceInput): this {
        const match = {"request":{"method":"PUT"}} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as BundleEntry
        const list = (this.resource.entry ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setPost (input: BatchBundle_Entry_PostSliceInput): this {
        const match = {"request":{"method":"POST"}} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as BundleEntry
        const list = (this.resource.entry ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setGet (input: BatchBundle_Entry_GetSliceInput): this {
        const match = {"request":{"method":"GET"}} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as BundleEntry
        const list = (this.resource.entry ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setDelete (input: BatchBundle_Entry_DeleteSliceInput): this {
        const match = {"request":{"method":"DELETE"}} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as BundleEntry
        const list = (this.resource.entry ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setPatch (input: BatchBundle_Entry_PatchSliceInput): this {
        const match = {"request":{"method":"PATCH"}} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as BundleEntry
        const list = (this.resource.entry ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setHead (input: BatchBundle_Entry_HeadSliceInput): this {
        const match = {"request":{"method":"HEAD"}} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as BundleEntry
        const list = (this.resource.entry ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public getPut (): BatchBundle_Entry_PutSliceInput | undefined {
        const match = {"request":{"method":"PUT"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["request"]) as BatchBundle_Entry_PutSliceInput
    }

    public getPutRaw (): BundleEntry | undefined {
        const match = {"request":{"method":"PUT"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getPost (): BatchBundle_Entry_PostSliceInput | undefined {
        const match = {"request":{"method":"POST"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["request"]) as BatchBundle_Entry_PostSliceInput
    }

    public getPostRaw (): BundleEntry | undefined {
        const match = {"request":{"method":"POST"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getGet (): BatchBundle_Entry_GetSliceInput | undefined {
        const match = {"request":{"method":"GET"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["request"]) as BatchBundle_Entry_GetSliceInput
    }

    public getGetRaw (): BundleEntry | undefined {
        const match = {"request":{"method":"GET"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getDelete (): BatchBundle_Entry_DeleteSliceInput | undefined {
        const match = {"request":{"method":"DELETE"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["request"]) as BatchBundle_Entry_DeleteSliceInput
    }

    public getDeleteRaw (): BundleEntry | undefined {
        const match = {"request":{"method":"DELETE"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getPatch (): BatchBundle_Entry_PatchSliceInput | undefined {
        const match = {"request":{"method":"PATCH"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["request"]) as BatchBundle_Entry_PatchSliceInput
    }

    public getPatchRaw (): BundleEntry | undefined {
        const match = {"request":{"method":"PATCH"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getHead (): BatchBundle_Entry_HeadSliceInput | undefined {
        const match = {"request":{"method":"HEAD"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["request"]) as BatchBundle_Entry_HeadSliceInput
    }

    public getHeadRaw (): BundleEntry | undefined {
        const match = {"request":{"method":"HEAD"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

}

