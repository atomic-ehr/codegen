// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Bundle } from "../../hl7-fhir-r5-core/Bundle";
import type { BundleEntry } from "../../hl7-fhir-r5-core/Bundle";

export type HistoryBundle_Entry_PutSliceInput = Omit<BundleEntry, "request"> & Required<Pick<BundleEntry, "fullUrl" | "resource" | "response">>;
export type HistoryBundle_Entry_PostSliceInput = Omit<BundleEntry, "request"> & Required<Pick<BundleEntry, "resource" | "response">>;
export type HistoryBundle_Entry_GetSliceInput = Omit<BundleEntry, "request"> & Required<Pick<BundleEntry, "fullUrl" | "response">>;
export type HistoryBundle_Entry_DeleteSliceInput = Omit<BundleEntry, "request"> & Required<Pick<BundleEntry, "fullUrl" | "response">>;

import { applySliceMatch, matchesSlice, extractSliceSimplified, validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/history-bundle (pkg: hl7.fhir.r5.core#5.0.0)
export class HistoryBundleProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/history-bundle"

    private resource: Bundle

    constructor (resource: Bundle) {
        this.resource = resource
        const r = resource as unknown as Record<string, unknown>
        const meta = (r.meta ??= {}) as Record<string, unknown>
        const profiles = (meta.profile ??= []) as string[]
        if (!profiles.includes("http://hl7.org/fhir/StructureDefinition/history-bundle")) profiles.push("http://hl7.org/fhir/StructureDefinition/history-bundle")
    }

    static from (resource: Bundle) : HistoryBundleProfile {
        return new HistoryBundleProfile(resource)
    }

    static createResource () : Bundle {
        const resource: Bundle = {
            resourceType: "Bundle",
            type: "history",
            meta: { profile: ["http://hl7.org/fhir/StructureDefinition/history-bundle"] },
        } as unknown as Bundle
        return resource
    }

    static create () : HistoryBundleProfile {
        return HistoryBundleProfile.from(HistoryBundleProfile.createResource())
    }

    toResource () : Bundle {
        return this.resource
    }

    getType () : ("document" | "message" | "transaction" | "transaction-response" | "batch" | "batch-response" | "history" | "searchset" | "collection") | undefined {
        return this.resource.type as ("document" | "message" | "transaction" | "transaction-response" | "batch" | "batch-response" | "history" | "searchset" | "collection") | undefined
    }

    setType (value: ("document" | "message" | "transaction" | "transaction-response" | "batch" | "batch-response" | "history" | "searchset" | "collection")) : this {
        Object.assign(this.resource, { type: value })
        return this
    }

    public setPut (input: HistoryBundle_Entry_PutSliceInput): this {
        const match = {"request":{"method":"PUT"}} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as BundleEntry
        const list = (this.resource.entry ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setPost (input: HistoryBundle_Entry_PostSliceInput): this {
        const match = {"request":{"method":"POST"}} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as BundleEntry
        const list = (this.resource.entry ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setGet (input: HistoryBundle_Entry_GetSliceInput): this {
        const match = {"request":{"method":"GET"}} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as BundleEntry
        const list = (this.resource.entry ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setDelete (input: HistoryBundle_Entry_DeleteSliceInput): this {
        const match = {"request":{"method":"DELETE"}} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as BundleEntry
        const list = (this.resource.entry ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public getPut (): HistoryBundle_Entry_PutSliceInput | undefined {
        const match = {"request":{"method":"PUT"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["request"]) as HistoryBundle_Entry_PutSliceInput
    }

    public getPutRaw (): BundleEntry | undefined {
        const match = {"request":{"method":"PUT"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getPost (): HistoryBundle_Entry_PostSliceInput | undefined {
        const match = {"request":{"method":"POST"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["request"]) as HistoryBundle_Entry_PostSliceInput
    }

    public getPostRaw (): BundleEntry | undefined {
        const match = {"request":{"method":"POST"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getGet (): HistoryBundle_Entry_GetSliceInput | undefined {
        const match = {"request":{"method":"GET"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["request"]) as HistoryBundle_Entry_GetSliceInput
    }

    public getGetRaw (): BundleEntry | undefined {
        const match = {"request":{"method":"GET"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getDelete (): HistoryBundle_Entry_DeleteSliceInput | undefined {
        const match = {"request":{"method":"DELETE"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["request"]) as HistoryBundle_Entry_DeleteSliceInput
    }

    public getDeleteRaw (): BundleEntry | undefined {
        const match = {"request":{"method":"DELETE"}} as Record<string, unknown>
        const list = this.resource.entry
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "type", "HistoryBundle"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "type", "history", "HistoryBundle"); if (e) errors.push(e) }
        { const e = validateEnum(r["type"], ["document","message","transaction","transaction-response","batch","batch-response","history","searchset","collection"], "type", "HistoryBundle"); if (e) errors.push(e) }
        errors.push(...validateSliceCardinality(r["entry"] as unknown[] | undefined, {"request":{"method":"POST"}}, "post", 0, 0, "HistoryBundle.entry"))
        return errors
    }

}

