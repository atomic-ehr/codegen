// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Attachment } from "../../hl7-fhir-r5-core/Attachment";
import type { CodeableConcept } from "../../hl7-fhir-r5-core/CodeableConcept";
import type { Extension } from "../../hl7-fhir-r5-core/Extension";
import type { Library } from "../../hl7-fhir-r5-core/Library";

export interface ModelInfoLibrary extends Library {
    url: string;
    version: string;
    title: string;
    description: string;
}

export type ModelInfoLibrary_ArtifactCommentInput = {
    type: string;
    text: string;
    target?: string[];
    reference?: string[];
    user?: string;
    authoredOn?: string;
}

export type ModelInfoLibrary_Content_ModelInfoXmlContentSliceInput = Omit<Attachment, "contentType"> & Required<Pick<Attachment, "data">>;
export type ModelInfoLibrary_Content_ModelInfoJsonContentSliceInput = Omit<Attachment, "contentType"> & Required<Pick<Attachment, "data">>;

import { applySliceMatch, matchesSlice, extractComplexExtension, extractSliceSimplified, validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

export type ModelInfoLibraryProfileParams = {
    url: string;
    version: string;
    title: string;
    description: string;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/modelinfolibrary (pkg: hl7.fhir.r5.core#5.0.0)
export class ModelInfoLibraryProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/modelinfolibrary"

    private resource: Library

    constructor (resource: Library) {
        this.resource = resource
        const r = resource as unknown as Record<string, unknown>
        const meta = (r.meta ??= {}) as Record<string, unknown>
        const profiles = (meta.profile ??= []) as string[]
        if (!profiles.includes("http://hl7.org/fhir/StructureDefinition/modelinfolibrary")) profiles.push("http://hl7.org/fhir/StructureDefinition/modelinfolibrary")
    }

    static from (resource: Library) : ModelInfoLibraryProfile {
        return new ModelInfoLibraryProfile(resource)
    }

    static createResource (args: ModelInfoLibraryProfileParams) : Library {
        const resource: Library = {
            resourceType: "Library",
            type: {"coding":[{"system":"http://terminology.hl7.org/CodeSystem/library-type","code":"model-definition"}]},
            url: args.url,
            version: args.version,
            title: args.title,
            description: args.description,
            meta: { profile: ["http://hl7.org/fhir/StructureDefinition/modelinfolibrary"] },
        } as unknown as Library
        return resource
    }

    static create (args: ModelInfoLibraryProfileParams) : ModelInfoLibraryProfile {
        return ModelInfoLibraryProfile.from(ModelInfoLibraryProfile.createResource(args))
    }

    toResource () : Library {
        return this.resource
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value })
        return this
    }

    getVersion () : string | undefined {
        return this.resource.version as string | undefined
    }

    setVersion (value: string) : this {
        Object.assign(this.resource, { version: value })
        return this
    }

    getTitle () : string | undefined {
        return this.resource.title as string | undefined
    }

    setTitle (value: string) : this {
        Object.assign(this.resource, { title: value })
        return this
    }

    getDescription () : string | undefined {
        return this.resource.description as string | undefined
    }

    setDescription (value: string) : this {
        Object.assign(this.resource, { description: value })
        return this
    }

    getType () : CodeableConcept<("logic-library" | "model-definition" | "asset-collection" | "module-definition" | string)> | undefined {
        return this.resource.type as CodeableConcept<("logic-library" | "model-definition" | "asset-collection" | "module-definition" | string)> | undefined
    }

    setType (value: CodeableConcept<("logic-library" | "model-definition" | "asset-collection" | "module-definition" | string)>) : this {
        Object.assign(this.resource, { type: value })
        return this
    }

    toProfile () : ModelInfoLibrary {
        return this.resource as ModelInfoLibrary
    }

    public setKnowledgeCapability (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeCapability", valueCode: value } as Extension)
        return this
    }

    public setKnowledgeRepresentationLevel (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeRepresentationLevel", valueCode: value } as Extension)
        return this
    }

    public setArtifactComment (input: ModelInfoLibrary_ArtifactCommentInput): this {
        const subExtensions: Extension[] = []
        if (input.type !== undefined) {
            subExtensions.push({ url: "type", valueCode: input.type })
        }
        if (input.text !== undefined) {
            subExtensions.push({ url: "text", valueMarkdown: input.text })
        }
        if (input.target) {
            for (const item of input.target) {
                subExtensions.push({ url: "target", valueUri: item })
            }
        }
        if (input.reference) {
            for (const item of input.reference) {
                subExtensions.push({ url: "reference", valueUri: item })
            }
        }
        if (input.user !== undefined) {
            subExtensions.push({ url: "user", valueString: input.user })
        }
        if (input.authoredOn !== undefined) {
            subExtensions.push({ url: "authoredOn", valueDateTime: input.authoredOn })
        }
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/cqf-artifactComment", extension: subExtensions })
        return this
    }

    public setModelInfoXmlContent (input: ModelInfoLibrary_Content_ModelInfoXmlContentSliceInput): this {
        const match = {"contentType":"application/xml"} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as Attachment
        const list = (this.resource.content ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setModelInfoJsonContent (input: ModelInfoLibrary_Content_ModelInfoJsonContentSliceInput): this {
        const match = {"contentType":"application/json"} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as Attachment
        const list = (this.resource.content ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public getKnowledgeCapability (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeCapability")
        return (ext as Record<string, unknown> | undefined)?.valueCode as string | undefined
    }

    public getKnowledgeCapabilityExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeCapability")
        return ext
    }

    public getKnowledgeRepresentationLevel (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeRepresentationLevel")
        return (ext as Record<string, unknown> | undefined)?.valueCode as string | undefined
    }

    public getKnowledgeRepresentationLevelExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeRepresentationLevel")
        return ext
    }

    public getArtifactComment (): ModelInfoLibrary_ArtifactCommentInput | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-artifactComment")
        if (!ext) return undefined
        const config = [{ name: "type", valueField: "valueCode", isArray: false }, { name: "text", valueField: "valueMarkdown", isArray: false }, { name: "target", valueField: "valueUri", isArray: true }, { name: "reference", valueField: "valueUri", isArray: true }, { name: "user", valueField: "valueString", isArray: false }, { name: "authoredOn", valueField: "valueDateTime", isArray: false }]
        return extractComplexExtension(ext as unknown as { extension?: Array<{ url?: string; [key: string]: unknown }> }, config) as ModelInfoLibrary_ArtifactCommentInput
    }

    public getArtifactCommentExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-artifactComment")
        return ext
    }

    public getModelInfoXmlContent (): ModelInfoLibrary_Content_ModelInfoXmlContentSliceInput | undefined {
        const match = {"contentType":"application/xml"} as Record<string, unknown>
        const list = this.resource.content
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["contentType"]) as ModelInfoLibrary_Content_ModelInfoXmlContentSliceInput
    }

    public getModelInfoXmlContentRaw (): Attachment | undefined {
        const match = {"contentType":"application/xml"} as Record<string, unknown>
        const list = this.resource.content
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    public getModelInfoJsonContent (): ModelInfoLibrary_Content_ModelInfoJsonContentSliceInput | undefined {
        const match = {"contentType":"application/json"} as Record<string, unknown>
        const list = this.resource.content
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        if (!item) return undefined
        return extractSliceSimplified(item as unknown as Record<string, unknown>, ["contentType"]) as ModelInfoLibrary_Content_ModelInfoJsonContentSliceInput
    }

    public getModelInfoJsonContentRaw (): Attachment | undefined {
        const match = {"contentType":"application/json"} as Record<string, unknown>
        const list = this.resource.content
        if (!list) return undefined
        const item = list.find((item) => matchesSlice(item, match))
        return item
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "url", "ModelInfoLibrary"); if (e) errors.push(e) }
        { const e = validateRequired(r, "version", "ModelInfoLibrary"); if (e) errors.push(e) }
        { const e = validateRequired(r, "title", "ModelInfoLibrary"); if (e) errors.push(e) }
        { const e = validateRequired(r, "description", "ModelInfoLibrary"); if (e) errors.push(e) }
        { const e = validateRequired(r, "type", "ModelInfoLibrary"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "type", {"coding":[{"system":"http://terminology.hl7.org/CodeSystem/library-type","code":"model-definition"}]}, "ModelInfoLibrary"); if (e) errors.push(e) }
        return errors
    }

}

