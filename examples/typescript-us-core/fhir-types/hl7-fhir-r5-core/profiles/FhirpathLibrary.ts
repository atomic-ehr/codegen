// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Attachment } from "../../hl7-fhir-r5-core/Attachment";
import type { Extension } from "../../hl7-fhir-r5-core/Extension";
import type { Library } from "../../hl7-fhir-r5-core/Library";
import type { RelatedArtifact } from "../../hl7-fhir-r5-core/RelatedArtifact";

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/fhirpathlibrary
export type FHIRPathLibrary_RelatedArtifact_DependencySliceInput = Omit<RelatedArtifact, "type"> & Required<Pick<RelatedArtifact, "resource">>;
export type FHIRPathLibrary_Content_FhirPathContentSliceInput = Omit<Attachment, "contentType"> & Required<Pick<Attachment, "data" | "id">>;

import { applySliceMatch, matchesSlice } from "../../profile-helpers";

export class FHIRPathLibraryProfile {
    private resource: Library

    constructor (resource?: Library) {
        this.resource = resource ?? ({ resourceType: "Library" } as Library)
    }

    toResource () : Library {
        return this.resource
    }

    public setKnowledgeCapability (value: Omit<Extension, "url">): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeCapability", ...value })
        return this
    }

    public setKnowledgeRepresentationLevel (value: Omit<Extension, "url">): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeRepresentationLevel", ...value })
        return this
    }

    public setArtifactComment (value: Omit<Extension, "url">): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/cqf-artifactComment", ...value })
        return this
    }

    public setDirectReferenceCode (value: Omit<Extension, "url">): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "http://hl7.org/fhir/StructureDefinition/cqf-directReferenceCode", ...value })
        return this
    }

    public setDependency (input: FHIRPathLibrary_RelatedArtifact_DependencySliceInput): this {
        const match = {"type":"depends-on"} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as RelatedArtifact
        const list = (this.resource.relatedArtifact ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public setFhirPathContent (input: FHIRPathLibrary_Content_FhirPathContentSliceInput): this {
        const match = {"contentType":"text/fhirpath"} as Record<string, unknown>
        const value = applySliceMatch(input as Record<string, unknown>, match) as unknown as Attachment
        const list = (this.resource.content ??= [])
        const index = list.findIndex((item) => matchesSlice(item, match))
        if (index === -1) {
            list.push(value)
        } else {
            list[index] = value
        }
        return this
    }

    public resetKnowledgeCapability (): this {
        const list = this.resource.extension
        if (list) {
            const index = list.findIndex((e) => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeCapability")
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

    public resetKnowledgeRepresentationLevel (): this {
        const list = this.resource.extension
        if (list) {
            const index = list.findIndex((e) => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-knowledgeRepresentationLevel")
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

    public resetArtifactComment (): this {
        const list = this.resource.extension
        if (list) {
            const index = list.findIndex((e) => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-artifactComment")
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

    public resetDirectReferenceCode (): this {
        const list = this.resource.extension
        if (list) {
            const index = list.findIndex((e) => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-directReferenceCode")
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

    public resetDependency (): this {
        const match = {"type":"depends-on"} as Record<string, unknown>
        const list = this.resource.relatedArtifact
        if (list) {
            const index = list.findIndex((item) => matchesSlice(item, match))
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

    public resetFhirPathContent (): this {
        const match = {"contentType":"text/fhirpath"} as Record<string, unknown>
        const list = this.resource.content
        if (list) {
            const index = list.findIndex((item) => matchesSlice(item, match))
            if (index !== -1) {
                list.splice(index, 1)
            }
        }
        return this
    }

}

