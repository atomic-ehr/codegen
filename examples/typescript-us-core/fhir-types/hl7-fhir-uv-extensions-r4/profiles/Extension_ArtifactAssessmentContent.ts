// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { CodeableConcept } from "../../hl7-fhir-r4-core/CodeableConcept";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Quantity } from "../../hl7-fhir-r4-core/Quantity";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";
import type { RelatedArtifact } from "../../hl7-fhir-r4-core/RelatedArtifact";

import { validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/artifactassessment-content (pkg: hl7.fhir.uv.extensions.r4#5.2.0)
export class ArtifactAssessmentContentProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/artifactassessment-content"

    private resource: Extension

    constructor (resource: Extension) {
        this.resource = resource
    }

    static from (resource: Extension) : ArtifactAssessmentContentProfile {
        return new ArtifactAssessmentContentProfile(resource)
    }

    static createResource () : Extension {
        const resource: Extension = {
            url: "http://hl7.org/fhir/StructureDefinition/artifactassessment-content",
        } as unknown as Extension
        return resource
    }

    static create () : ArtifactAssessmentContentProfile {
        return ArtifactAssessmentContentProfile.from(ArtifactAssessmentContentProfile.createResource())
    }

    toResource () : Extension {
        return this.resource
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value })
        return this
    }

    public setInformationType (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "informationType", valueCode: value } as Extension)
        return this
    }

    public setSummary (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "summary", valueMarkdown: value } as Extension)
        return this
    }

    public setType (value: CodeableConcept): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "type", valueCodeableConcept: value } as Extension)
        return this
    }

    public setClassifier (value: CodeableConcept): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "classifier", valueCodeableConcept: value } as Extension)
        return this
    }

    public setQuantity (value: Quantity): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "quantity", valueQuantity: value } as Extension)
        return this
    }

    public setAuthor (value: Reference): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "author", valueReference: value } as Extension)
        return this
    }

    public setPath (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "path", valueUri: value } as Extension)
        return this
    }

    public setRelatedArtifact (value: RelatedArtifact): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "relatedArtifact", valueRelatedArtifact: value } as Extension)
        return this
    }

    public setFreeToShare (value: boolean): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "freeToShare", valueBoolean: value } as Extension)
        return this
    }

    public setComponent (value: Omit<Extension, "url">): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "component", ...value })
        return this
    }

    public getInformationType (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "informationType")
        return (ext as Record<string, unknown> | undefined)?.valueCode as string | undefined
    }

    public getInformationTypeExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "informationType")
        return ext
    }

    public getSummary (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "summary")
        return (ext as Record<string, unknown> | undefined)?.valueMarkdown as string | undefined
    }

    public getSummaryExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "summary")
        return ext
    }

    public getType (): CodeableConcept | undefined {
        const ext = this.resource.extension?.find(e => e.url === "type")
        return (ext as Record<string, unknown> | undefined)?.valueCodeableConcept as CodeableConcept | undefined
    }

    public getTypeExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "type")
        return ext
    }

    public getClassifier (): CodeableConcept | undefined {
        const ext = this.resource.extension?.find(e => e.url === "classifier")
        return (ext as Record<string, unknown> | undefined)?.valueCodeableConcept as CodeableConcept | undefined
    }

    public getClassifierExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "classifier")
        return ext
    }

    public getQuantity (): Quantity | undefined {
        const ext = this.resource.extension?.find(e => e.url === "quantity")
        return (ext as Record<string, unknown> | undefined)?.valueQuantity as Quantity | undefined
    }

    public getQuantityExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "quantity")
        return ext
    }

    public getAuthor (): Reference | undefined {
        const ext = this.resource.extension?.find(e => e.url === "author")
        return (ext as Record<string, unknown> | undefined)?.valueReference as Reference | undefined
    }

    public getAuthorExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "author")
        return ext
    }

    public getPath (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "path")
        return (ext as Record<string, unknown> | undefined)?.valueUri as string | undefined
    }

    public getPathExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "path")
        return ext
    }

    public getRelatedArtifact (): RelatedArtifact | undefined {
        const ext = this.resource.extension?.find(e => e.url === "relatedArtifact")
        return (ext as Record<string, unknown> | undefined)?.valueRelatedArtifact as RelatedArtifact | undefined
    }

    public getRelatedArtifactExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "relatedArtifact")
        return ext
    }

    public getFreeToShare (): boolean | undefined {
        const ext = this.resource.extension?.find(e => e.url === "freeToShare")
        return (ext as Record<string, unknown> | undefined)?.valueBoolean as boolean | undefined
    }

    public getFreeToShareExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "freeToShare")
        return ext
    }

    public getComponent (): Extension | undefined {
        return this.resource.extension?.find(e => e.url === "component")
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "url", "ArtifactAssessmentContent"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "url", "http://hl7.org/fhir/StructureDefinition/artifactassessment-content", "ArtifactAssessmentContent"); if (e) errors.push(e) }
        return errors
    }

}

