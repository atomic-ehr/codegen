// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";

import { validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/derivation-reference (pkg: hl7.fhir.uv.extensions.r4#5.2.0)
export class ResourceDerivationReferenceProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/derivation-reference"

    private resource: Extension

    constructor (resource: Extension) {
        this.resource = resource
    }

    static from (resource: Extension) : ResourceDerivationReferenceProfile {
        return new ResourceDerivationReferenceProfile(resource)
    }

    static createResource () : Extension {
        const resource: Extension = {
            url: "http://hl7.org/fhir/StructureDefinition/derivation-reference",
        } as unknown as Extension
        return resource
    }

    static create () : ResourceDerivationReferenceProfile {
        return ResourceDerivationReferenceProfile.from(ResourceDerivationReferenceProfile.createResource())
    }

    toResource () : Extension {
        return this.resource
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value })
        return this
    }

    public setReference (value: Reference): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "reference", valueReference: value } as Extension)
        return this
    }

    public setPath (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "path", valueString: value } as Extension)
        return this
    }

    public setOffset (value: number): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "offset", valueInteger: value } as Extension)
        return this
    }

    public setLength (value: number): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "length", valueInteger: value } as Extension)
        return this
    }

    public getReference (): Reference | undefined {
        const ext = this.resource.extension?.find(e => e.url === "reference")
        return (ext as Record<string, unknown> | undefined)?.valueReference as Reference | undefined
    }

    public getReferenceExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "reference")
        return ext
    }

    public getPath (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "path")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getPathExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "path")
        return ext
    }

    public getOffset (): number | undefined {
        const ext = this.resource.extension?.find(e => e.url === "offset")
        return (ext as Record<string, unknown> | undefined)?.valueInteger as number | undefined
    }

    public getOffsetExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "offset")
        return ext
    }

    public getLength (): number | undefined {
        const ext = this.resource.extension?.find(e => e.url === "length")
        return (ext as Record<string, unknown> | undefined)?.valueInteger as number | undefined
    }

    public getLengthExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "length")
        return ext
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "url", "ResourceDerivationReference"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "url", "http://hl7.org/fhir/StructureDefinition/derivation-reference", "ResourceDerivationReference"); if (e) errors.push(e) }
        return errors
    }

}

