// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Expression } from "../../hl7-fhir-r4-core/Expression";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import { validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

export type TargetConstraintProfileParams = {
    extension: Extension[];
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/targetConstraint (pkg: hl7.fhir.uv.extensions.r4#5.2.0)
export class TargetConstraintProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/targetConstraint"

    private resource: Extension

    constructor (resource: Extension) {
        this.resource = resource
    }

    static from (resource: Extension) : TargetConstraintProfile {
        return new TargetConstraintProfile(resource)
    }

    static createResource (args: TargetConstraintProfileParams) : Extension {
        const resource: Extension = {
            url: "http://hl7.org/fhir/StructureDefinition/targetConstraint",
            extension: args.extension,
        } as unknown as Extension
        return resource
    }

    static create (args: TargetConstraintProfileParams) : TargetConstraintProfile {
        return TargetConstraintProfile.from(TargetConstraintProfile.createResource(args))
    }

    toResource () : Extension {
        return this.resource
    }

    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value })
        return this
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value })
        return this
    }

    public setKey (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "key", valueId: value } as Extension)
        return this
    }

    public setRequirements (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "requirements", valueMarkdown: value } as Extension)
        return this
    }

    public setSeverity (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "severity", valueCode: value } as Extension)
        return this
    }

    public setExpression (value: Expression): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "expression", valueExpression: value } as Extension)
        return this
    }

    public setHuman (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "human", valueString: value } as Extension)
        return this
    }

    public setLocation (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "location", valueString: value } as Extension)
        return this
    }

    public getKey (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "key")
        return (ext as Record<string, unknown> | undefined)?.valueId as string | undefined
    }

    public getKeyExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "key")
        return ext
    }

    public getRequirements (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "requirements")
        return (ext as Record<string, unknown> | undefined)?.valueMarkdown as string | undefined
    }

    public getRequirementsExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "requirements")
        return ext
    }

    public getSeverity (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "severity")
        return (ext as Record<string, unknown> | undefined)?.valueCode as string | undefined
    }

    public getSeverityExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "severity")
        return ext
    }

    public getExpression (): Expression | undefined {
        const ext = this.resource.extension?.find(e => e.url === "expression")
        return (ext as Record<string, unknown> | undefined)?.valueExpression as Expression | undefined
    }

    public getExpressionExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "expression")
        return ext
    }

    public getHuman (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "human")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getHumanExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "human")
        return ext
    }

    public getLocation (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "location")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getLocationExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "location")
        return ext
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "extension", "TargetConstraint"); if (e) errors.push(e) }
        { const e = validateRequired(r, "url", "TargetConstraint"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "url", "http://hl7.org/fhir/StructureDefinition/targetConstraint", "TargetConstraint"); if (e) errors.push(e) }
        return errors
    }

}

