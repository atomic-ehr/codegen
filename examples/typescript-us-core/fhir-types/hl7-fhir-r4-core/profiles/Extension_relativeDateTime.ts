// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Duration } from "../../hl7-fhir-r4-core/Duration";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";

import { validateRequired, validateExcluded, validateFixedValue, validateSliceCardinality, validateEnum, validateReference } from "../../profile-helpers";

export type relativeDateTimeProfileParams = {
    extension: Extension[];
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/cqf-relativeDateTime (pkg: hl7.fhir.r4.core#4.0.1)
export class relativeDateTimeProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/cqf-relativeDateTime"

    private resource: Extension

    constructor (resource: Extension) {
        this.resource = resource
    }

    static from (resource: Extension) : relativeDateTimeProfile {
        return new relativeDateTimeProfile(resource)
    }

    static createResource (args: relativeDateTimeProfileParams) : Extension {
        const resource: Extension = {
            url: "http://hl7.org/fhir/StructureDefinition/cqf-relativeDateTime",
            extension: args.extension,
        } as unknown as Extension
        return resource
    }

    static create (args: relativeDateTimeProfileParams) : relativeDateTimeProfile {
        return relativeDateTimeProfile.from(relativeDateTimeProfile.createResource(args))
    }

    toResource () : Extension {
        return this.resource
    }

    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value })
        return this
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value })
        return this
    }

    public setTarget (value: Reference): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "target", valueReference: value } as Extension)
        return this
    }

    public setTargetPath (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "targetPath", valueString: value } as Extension)
        return this
    }

    public setRelationship (value: string): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "relationship", valueCode: value } as Extension)
        return this
    }

    public setOffset (value: Duration): this {
        const list = (this.resource.extension ??= [])
        list.push({ url: "offset", valueDuration: value } as Extension)
        return this
    }

    public getTarget (): Reference | undefined {
        const ext = this.resource.extension?.find(e => e.url === "target")
        return (ext as Record<string, unknown> | undefined)?.valueReference as Reference | undefined
    }

    public getTargetExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "target")
        return ext
    }

    public getTargetPath (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "targetPath")
        return (ext as Record<string, unknown> | undefined)?.valueString as string | undefined
    }

    public getTargetPathExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "targetPath")
        return ext
    }

    public getRelationship (): string | undefined {
        const ext = this.resource.extension?.find(e => e.url === "relationship")
        return (ext as Record<string, unknown> | undefined)?.valueCode as string | undefined
    }

    public getRelationshipExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "relationship")
        return ext
    }

    public getOffset (): Duration | undefined {
        const ext = this.resource.extension?.find(e => e.url === "offset")
        return (ext as Record<string, unknown> | undefined)?.valueDuration as Duration | undefined
    }

    public getOffsetExtension (): Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "offset")
        return ext
    }

    validate () : string[] {
        const errors: string[] = []
        const r = this.resource as unknown as Record<string, unknown>
        { const e = validateRequired(r, "extension", "relativeDateTime"); if (e) errors.push(e) }
        { const e = validateRequired(r, "url", "relativeDateTime"); if (e) errors.push(e) }
        { const e = validateFixedValue(r, "url", "http://hl7.org/fhir/StructureDefinition/cqf-relativeDateTime", "relativeDateTime"); if (e) errors.push(e) }
        return errors
    }

}

